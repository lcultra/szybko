{
    "name": "@szybko/core-rust",
    "version": "0.1.0",
    "private": true,
    "scripts": {
        "build": "napi build --release --platform",
        "build:debug": "napi build --platform"
    },
    "napi": {
        "name": "szybko-core",
        "triples": {
            "defaults": true
        }
    },
    "devDependencies": {
        "@napi-rs/cli": "^3.0"
    }
}
